# 安全性

## 先講清楚這個 repo 是什麼

這是一個**公司內部的 side project**，不是對外提供的服務，也沒有公開的部署。
沒有維運團隊、沒有 SLA、沒有資安響應流程。

所以這份文件不是「我們的安全政策」，而是兩件事：**怎麼回報**，以及
**哪些東西不是漏洞、是知情的取捨**。

## 怎麼回報

請走 GitHub 的 **Private vulnerability reporting**（這個 repo 的 Security 分頁
→ Report a vulnerability）。不要開公開 issue。

刻意不在這裡放 email：這個 repo 是公開的，而放一個真人信箱進來只會被爬走。

沒有獎金，也不保證回應時間 —— 見上面那句「沒有維運團隊」。

## 哪些不是漏洞

這個專案的設計文件記錄了幾個**已知而且刻意的**弱點。回報它們不會得到修復，
因為它們不是意外：

| 你可能會看到 | 它是什麼 |
|---|---|
| 代跑者的長期 OAuth token 存在共用主機上 | 知情的取捨，完整論述在 [SPEC.md](SPEC.md) §1。舊模型（憑證不離開代跑者機器）在合規上更強，是為了「沒有人需要安裝任何東西」換掉的 |
| job 內容對主機管理者可見 | 同上。系統不宣稱「沒有人看得到」—— 宣稱看不到才是虛假安全感 |
| 早期 commit 裡有內部服務位址與 service id | 已從程式碼移除（預設值改成必填），但**歷史沒有重寫**。它們不是憑證 |
| 借用者不能選權限模式 | 刻意的，理由在 [docs/web-spec.md](docs/web-spec.md) §10。`--allowedTools` 是固定的三個 |

**這些會想聽**：憑證真的洩漏的路徑（進 log、進 image、回傳到前端）、
job 之間的隔離被繞過、egress 白名單被繞過、能讀到別人 job 內容的存取控制缺口。
那四條寫在 [.claude/rules/security.md](.claude/rules/security.md)，是這個專案
自己畫的紅線 —— 踩到紅線的才算漏洞。

## 這個 repo 本身

不接受外部部署，也不建議。登入綁在內部認證服務上，沒有其他登入方式。
