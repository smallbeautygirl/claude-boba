# claude-boba

同事之間的 Claude 代跑互助平台。這份檔案只定義這個專案裡的東西**叫什麼**；
每個決定的理由在 [SPEC.md](SPEC.md)，畫面長相在 [docs/web-spec.md](docs/web-spec.md)。

## Language

### 通知

**通知目的地**：
一個人的通知會送到哪裡。每個人恰好有一個，不是開關 —— 沒有「不通知」這個狀態。
_Avoid_：通知開關、開啟／關閉通知、有沒有設定通知

**頻道通知**：
零設定的預設目的地。送到共用頻道並 `@` 當事人，不含金額。
頻道由 hub 設定（`TEAMS_CHANNEL_WEBHOOK` / `TEAMS_CHANNEL_NAME`），不是每個人各自選的。
_Avoid_：預設通知、公開通知

**私訊通知**：
當事人填了自己的 webhook 之後的目的地。只有他看得到，含金額。
_Avoid_：個人通知、自訂通知

### 輸入與延續

**附件**：
借用者上傳、要被這個 job 處理的檔案。job 跑起來時它們就在工作目錄根層。
_Avoid_：上傳的檔案、輸入檔

**session 檔**：
Claude Code 在借用者自己機器上留下的 `.jsonl`。帶著它的 job 是**對話的延續**，
不是拿到一份新的標的 —— 這是它與附件的根本差別，不是上傳方式的差別。
_Avoid_：對話檔、歷史檔

**job transcript**：
job 自己跑完留下的對話紀錄，「接著問」靠它。方向與 session 檔相反：
session 檔是借用者帶進來的，job transcript 是 job 產生的。
_Avoid_：單講「transcript」而不指明是哪一個

### 提交頁

**輸入列**：
提交頁上「寫 prompt + 加東西 + 送出」的那一整條。裡面只放**動作**。
_Avoid_：對話框（這裡不是對話，是一次性的提交）、composer

**job 參數**：
送出前決定這個 job 怎麼跑的東西，目前只有 model。它不是動作，所以不進輸入列 ——
它決定這個 job 花多少錢，而錢要被看見。
_Avoid_：設定、選項

### 指令

**指令**：
委託者在輸入列上選的那一個能力，畫面上以動詞短句呈現（「做簡報」「拷問我的想法」）。
一個 job 只有一個 —— worker 把整個文字框當成一個 prompt，Claude Code 只認開頭那一個。
_Avoid_：skill（那是實作的詞，要懂產品架構才看得懂）、快捷、外掛

**指令清單**：
hub 提供、前端顯示的那份**被編輯過的推薦**。
**它不等於 job 容器裡實際有的東西** —— 容器裡的比它多，而清單只列有中文文案的那些。
_Avoid_：把它當成庫存清冊、完整清單

**服務憑證**：
委託者為了讓某個指令連得到外部服務而交出的憑證，每人一份。
與**代跑 token** 方向相反：代跑 token 是代跑者交的、燒的是他的額度；
服務憑證是委託者交的、動的是他自己在那個服務上的權限。
_Avoid_：`.env`、API key、密碼

**指令宣告**：
一個指令放在自己目錄旁的 `boba.json`，宣告它要什麼：對外網域、服務憑證、怎麼驗。
文案與分組不在裡面 —— 那是跨指令的編輯判斷，不屬於任何一個指令。
_Avoid_：manifest、設定檔

### 出租端

**代跑者**：
把自己的 Claude 額度借出去、讓別人的 job 用它跑完的人。
_Avoid_：出租者（那是「租借帳號」的語彙，而這個系統刻意不是那件事）、供給者

**委託者**：
把 job 丟進來、用別人額度跑完的人。
_Avoid_：借用者、需求者、使用者（兩種角色都是使用者）

> 2026-09-22 起改用這組詞。舊文件（SPEC.md、程式碼註解、板子）裡的
> **出租者＝代跑者**、**借用者＝委託者**，那些文件維持原樣不改寫 ——
> 它們記錄的是當時的決策，不需要跟現在的詞彙一致。

**出借設定**：
一位代跑者出借額度的**條件**：花費上限、可用 model、要不要開放外網、現在接不接單。
每位代跑者恰好一份 —— 這裡的「一份」指的是一組條件，不是一個帳號。
他借出兩個**出借帳號**時，條件仍然只有一份：上限 US$5 講的是他對風險的態度，
不是他對某個帳號的態度。
_Avoid_：worker、我的 worker、額度來源（那是系統看它的角度，不是人看它的角度）

**出借帳號**：
被借出去的那個 Claude 帳號本身：持有一組**代跑 token**，有自己的額度、
rate limit 與重置時間。一位代跑者可以有多個（例如個人帳號、公司配給他的帳號），
它們共用同一份**出借設定**。

對**委託者**完全不可見 —— 他挑的是人，站台自己決定這個 job 用哪個帳號跑。
所以它只在一個地方露臉：代跑者自己的「我來代跑」頁。
_Avoid_：worker（那是主機上那支常駐程式）、額度來源、
把它跟代跑 token 混為一談（token 是憑證，會過期、會被撤銷、會換新；
出借帳號是持有它的那個東西，換了 token 還是同一個帳號）

**worker**：
**主機上那個常駐程式**，負責領單並啟動 job 容器。整個站台只有一個。
這個詞**不對使用者使用** —— 代跑者沒有 worker，他有的是出借設定。
_Avoid_：拿 worker 指稱某位代跑者、或指稱他的那筆記錄

**job 容器**：
每個 job 臨時開、跑完即銷毀的那個 Docker 容器。與 worker 的生命週期完全不同：
worker 長期存在，job 容器只活一個 job。
_Avoid_：worker 容器、執行環境

**代跑 token**：
代跑者授權給這個站台、用來以他的額度執行 job 的長期 OAuth token
（`claude setup-token` 產生，一年有效）。**單向** —— 交進來之後任何介面都不再顯示它。
_Avoid_：API key、密碼；也不要用「Claude 帳號」指稱 token 本身 —— 帳號是**出借帳號**，
token 只是它交出來的那張憑證

**出租者憑證**（舊模型，仍支援）：
代跑者自己機器上的 Claude Code 登入憑證檔。它不經過網頁、不進 hub、不進 image，
由 job 容器以唯讀掛入。這是「在自己機器上代跑」那條路，與代跑 token 並存。
_Avoid_：把它跟代跑 token 混為一談 —— 兩者的通道與風險完全不同

**沒有目的地**：
hub 未設共用頻道、當事人也沒填 webhook 時的狀態。這時他收不到任何通知，
只能從網頁自己看。這是三種目的地之一，不是錯誤狀態。
_Avoid_：通知關閉、未設定

### 派單

**自動**：
提交頁代跑者欄位的預設值。站台自己從「線上 ∧ 接單中 ∧ 有可用帳號」的代跑者裡挑，
**而且排除委託者本人** —— 這一頁的說法是「找還有額度的**同事**幫你跑」，
派給自己不是互助。
_Avoid_：隨機、自動分配（它不是隨機的，挑的是額度最充裕的帳號）

**指定自己**：
委託者在代跑者欄位選了自己。**這是允許的，而且不掛債** ——
個人帳號額度爆了、公司帳號還有，那正是一個人有多個出借帳號的場景。
自動不會派給你，所以這是你跑到自己的唯一入口。
_Avoid_：自借自還、借給自己（沒有借這回事，那一趟燒的本來就是自己的額度）

**需重新授權**：
一個出借帳號的代跑 token 失效了（過期、被撤銷、帳號被收回）的狀態。
**只有那一個帳號停止接單，同一位代跑者的其他帳號照常** —— job 不受影響。
_Avoid_：帳號壞掉、帳號停用（聽起來像是帳號本身出事，實際上只是那張憑證過期）

### 許願板

**許願板**：
收「這個產品哪裡不好用、我還想要什麼」的那面牆。讀的人是維護者。
它是**試玩期的鷹架**，有下架條件（web-spec §12），不是長期功能。
_Avoid_：回饋板（沒有人想貼一個叫「回饋」的東西）、動態牆（那是 §7 否決掉的另一件事，
它洩漏的是「誰跟誰借了什麼」；許願板上只有人自己選擇要說的話）、意見箱（那是私下的）

**願望**：
牆上的一則。有分類、內文、署名，可以貼圖。
**沒有「處理中」這個狀態** —— 願望只有兩種：還沒實現的，和**實現了**的。
系統不記錄任何人打算做什麼，只記錄已經發生的事。有人在留言裡說「我來做」是人講的話，
不會變成牆上的標籤。
_Avoid_：**接單**（那是**出借設定**裡「現在接不接單」的詞，指代跑者願不願意收 job，
跟這裡完全無關）、工單、issue、需求單、待辦

**分類**：
一則願望屬於四類之一：壞掉了／想要一個指令／體驗不好／其他。
它的作用只有兩個：牆上的篩選，以及新願望廣播的文案 —— **不分流給不同的人**，
因為讀的人只有一個。「想要一個指令」是唯一接得到既有機制的一類（SPEC §4.13）。
_Avoid_：標籤（一則只有一個分類，不是多選）、類型

**實現了**：
一則願望被做掉的狀態。任何人都能標，但**必須附 PR 或 commit 連結**才標得了 ——
沒有連結，「實現了」就退化成空頭宣告。可以取消。
_Avoid_：完成、關閉、已解決（那些詞來自工單系統，會讓人以為還有「處理中」）

**反應**：
按在願望或留言上的 emoji。一個人可以按多顆、同一顆再按一次是取消。
**只顯示數字，不顯示是誰按的** —— 跟排行榜只顯示聚合數字是同一條線。
所有 emoji 等值，系統不替 😭 和 🎉 決定誰比較重。
_Avoid_：讚、+1（那是單一一顆的語彙，這裡任何 emoji 都算一次）、投票

**新願望廣播**：
有人貼了新願望時送到共用頻道的那一行（只有一句話與連結，不含內容）。
**它不是通知目的地**：通知目的地是「**一個人**的通知會送到哪裡，每人恰好一個」，
而這則廣播沒有特定當事人。它直接用 `TEAMS_CHANNEL_WEBHOOK`。
留言與「實現了」則相反 —— 那兩件事有明確的當事人（貼文者），走的是通知目的地。
_Avoid_：把它叫成頻道通知（那是通知目的地的一種）
