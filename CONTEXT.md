# claude-boba

同事之間的 Claude 代跑互助平台。這份檔案只定義這個專案裡的東西**叫什麼**；
每個決定的理由在 [SPEC.md](SPEC.md)，畫面長相在 [docs/web-spec.md](docs/web-spec.md)。

## Language

### 通知

**通知目的地**：
一個人的通知會送到哪裡。每個人恰好有一個，不是開關 —— 沒有「不通知」這個狀態。
_Avoid_：通知開關、開啟／關閉通知、有沒有設定通知

**頻道通知**：
零設定的預設目的地。送到共用頻道並 `@` 當事人，不含金額。
頻道由 hub 設定（`TEAMS_CHANNEL_WEBHOOK` / `TEAMS_CHANNEL_NAME`），不是每個人各自選的。
_Avoid_：預設通知、公開通知

**私訊通知**：
當事人填了自己的 webhook 之後的目的地。只有他看得到，含金額。
_Avoid_：個人通知、自訂通知

**沒有目的地**：
hub 未設共用頻道、當事人也沒填 webhook 時的狀態。這時他收不到任何通知，
只能從網頁自己看。這是三種目的地之一，不是錯誤狀態。
_Avoid_：通知關閉、未設定
