說明 claude-boba 這個代跑平台目前提供哪些能力，用條列，簡短。

必須包含：

- 這個 job 跑在別人的電腦上，對方技術上看得到你送的內容
- 內建可用的 slash command（例如 /pptx 做簡報或改既有簡報；表格、Word、PDF 目前還沒有對應的指令）
- 網路可能是白名單模式，pip install 與抓網頁可能會失敗
- 跑完可以「接著問」，會沿用同一個對話上下文
